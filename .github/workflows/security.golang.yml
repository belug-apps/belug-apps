---
name: Security (Golang)
on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, synchronize]
    paths: ["src/api-proxy/**/*"]
  push:
    branches: [main]

jobs:
  check_golang_issue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@755da8c3cf115ac066823e79a1e1788f8940201b # v3.2.0
      - uses: snyk/actions/golang@7fad562681122205233d1242c3bb39598c5393da # tag=0.3.0
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --sarif-file-output=snyk.sarif src/api-proxy/
      - uses: github/codeql-action/upload-sarif@bdc7c5d203860b808ad2371e491a35160fc2f9c0 # tag=codeql-bundle-20221024
        with:
          sarif_file: snyk.sarif
